function solve() {
    let btnElement = document.querySelector('#container button');
    let inputElements = document.querySelectorAll('#container input');
    let inputs = Array.from(inputElements);


    btnElement.addEventListener('click', e => {
        e.preventDefault();

    })
}

